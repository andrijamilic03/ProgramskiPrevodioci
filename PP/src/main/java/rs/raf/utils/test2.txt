целобројни x = 10;
логички y = истина;
целобројни низ[2][3];

низ[0][0] = x;
низ[0][1] = x + 5;
низ[0][2] = x - 3;

ако (x > 5 && y == истина) {
    x = x * 2;
} иначе {
    x = 0;
}

штампај(x);       // Треба да испише 20
штампај(низ[0][1]); // Треба да испише 15
штампај(низ[0][2]); // Треба да испише 7
